#include <stdio.h>
#include <string.h>

const int max_chars = 50;

// Declaracion de estructuras.
typedef struct {
    char zona[max_chars];
    char calle[max_chars];
    char colonia[max_chars];
} ubicacion;

typedef struct {
    char clave[max_chars];
    float sup_cubierta;
    float sup_terreno;
    char caracteristicas[max_chars];
} propiedades;

// Programa principal.
int main() {

    return 0;
}